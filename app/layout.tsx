import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Chem Lab - Virtual Science Experiment Simulation",
  description:
    "Interactive virtual chemistry laboratory for educational experiments with AI assistance, safety protocols, and progress tracking.",
  keywords: "chemistry, virtual lab, education, experiments, simulation, science learning",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
